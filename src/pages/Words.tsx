import WordsGame from "@/features/words/WordsGame";

/**
 * /words — the word-wheel crossword mode. Everything lives in
 * src/features/words; this is only the route's entry point.
 */
export default function Words() {
  return <WordsGame />;
}
