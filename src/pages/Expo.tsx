import ExpoGame from "@/features/expo/ExpoGame";

/**
 * /expo — the experimental word-wheel mode. Everything lives in
 * src/features/expo; this is only the route's entry point.
 */
export default function Expo() {
  return <ExpoGame />;
}
