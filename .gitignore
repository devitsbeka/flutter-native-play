# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript incremental build state (written by `tsc -b`)
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cloudflare Wrangler local dev state
.wrangler/

# Playwright output
test-results/
playwright-report/
blob-report/
.playwright/

# .env IS tracked, deliberately — see the note at the top of .env.example.
# It holds only the Supabase project ref and the publishable (anon) key, both
# public by design, and three separate build systems read it.
.env.local
.env.*.local

# Snapshots of the live question bank, pulled by scripts/audit-questions.mjs and
# rebuilt by scripts/question-repair. Derived data that goes stale — the
# hand-authored rewrites.jsonl is the part worth keeping.
scripts/question-repair/en_raw.jsonl
scripts/question-repair/ka_raw.jsonl
scripts/question-repair/categories.json
scripts/question-repair/worklist.json
scripts/question-repair/residue.json
scripts/question-repair/migration.sql
scripts/question-repair/tiers.json
scripts/question-repair/priority.json
scripts/question-repair/image-cache.json
scripts/question-repair/image-questions.json
scripts/question-repair/staged_ids.json
scripts/question-repair/build.log
