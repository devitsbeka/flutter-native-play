# Deploy revenuecat-webhook Edge Function

## Goal
Deploy the already-written `revenuecat-webhook` edge function from `supabase/functions/revenuecat-webhook/` to the Lovable Cloud backend.

## Plan
1. Run `supabase--deploy_edge_functions` for `["revenuecat-webhook"]`.
2. Report the deployment result.

## Constraints
- Do not create, modify, rewrite, or regenerate any files.
- Do not regenerate `src/integrations/supabase/types.ts`.
- No application code changes.
